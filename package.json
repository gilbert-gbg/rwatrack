{
  "name": "rwa-track-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "mobile": "cd mobile && npx expo start",
    "web": "cd web && npm run dev",
    "install:all": "cd mobile && npm install && cd ../web && npm install"
  },
  "dependencies": {
    "axios": "^1.16.0"
  }
}
